{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "build": {
      "commands": [
        "...",
        "python manage.py collectstatic --noinput"
      ]
    }
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn astro_project.wsgi:application --bind 0.0.0.0:$PORT"
  }
}
